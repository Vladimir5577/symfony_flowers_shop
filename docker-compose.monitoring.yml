# Мониторинг: Grafana + Prometheus + postgres-exporter.
# Общение по внутренней сети Docker. Подключается к сети основного проекта (app-net).
#
# Запуск (сначала поднять основной проект):
#   docker compose up -d
#   docker compose -f docker-compose.monitoring.yml up -d
#
# Grafana: http://localhost:3030 (admin / admin)
# Prometheus: http://localhost:9090
# В Grafana: Data source → Prometheus → URL: http://prometheus:9090

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./docker_env/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=15d
      - --web.enable-lifecycle
    ports:
      - "9090:9090"
    networks:
      - app-net
      - monitoring-net
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SERVER_ROOT_URL: http://localhost:3030
    volumes:
      - grafana_data:/var/lib/grafana
    ports:
      - "3030:3000"
    networks:
      - monitoring-net
    restart: unless-stopped

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: postgres-exporter
    environment:
      DATA_SOURCE_NAME: "postgresql://${DB_USERNAME}:${DB_PASSWORD}@postgres:5432/${DB_DATABASE}?sslmode=disable"
    ports:
      - "9187:9187"
    networks:
      - app-net
    restart: unless-stopped

  nginx-exporter:
    image: nginx/nginx-prometheus-exporter:latest
    container_name: nginx-exporter
    command:
      - -nginx.scrape-uri=http://nginx_web:80/nginx_status
    ports:
      - "9113:9113"
    networks:
      - app-net
    restart: unless-stopped

networks:
  app-net:
    name: app-net
    external: true
  monitoring-net:
    driver: bridge

volumes:
  prometheus_data: {}
  grafana_data: {}
